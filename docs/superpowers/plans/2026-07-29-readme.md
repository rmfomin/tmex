# README Update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the obsolete README with an English guide for building, loading, and using Tablo.

**Architecture:** Documentation-only change. The README becomes the public entry point and derives every command from `package.json` and every feature from the current extension manifest and source structure.

**Tech Stack:** Markdown, npm, Webpack, Chrome Extensions Manifest V3.

## Global Constraints

- The README is written in English.
- It must not describe TMex, the old source layout, or network synchronization.
- It must document only existing project commands and features.
- Do not stage or commit files without explicit user permission.

### Task 1: Replace the README

**Files:**
- Modify: `README.md`
- Reference: `package.json`
- Reference: `public/manifest-normal.json`

**Interfaces:**
- Consumes: npm scripts and Manifest V3 settings from the referenced files.
- Produces: an English Markdown guide for local Chrome installation and development.

- [x] **Step 1: Replace obsolete content with the new guide**

Include these sections in order:

    # Tablo
    ## Features
    ## Requirements
    ## Install and run locally
    ## Production build
    ## Available commands

The local-run steps are `npm install`, `npm run watch`, and selecting `dist` through `chrome://extensions` with Developer mode enabled. The production-build step is `npm run build` and selecting `dist`.

- [x] **Step 2: Verify command names and output directory**

Run:

    node -e 'const p=require("./package.json"); for (const key of ["watch","watch2","build","typecheck","test","clean"]) if (!p.scripts[key]) throw new Error(`missing script: ${key}`);'
    test -f dist/manifest.json

Expected: all commands exist and the Chrome-loadable output contains `dist/manifest.json`.

- [x] **Step 3: Inspect the documentation change**

Run:

    git diff --check
    git diff -- README.md

Expected: no whitespace errors; README contains only the documented, current project behavior.
